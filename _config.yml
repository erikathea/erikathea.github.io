# Site settings
title: CodeTravel
email: erikatheaajes@gmail.com
description: > # this means to ignore newlines until "baseurl:"
  Weird/nerd Asian dudette. Programmer. Backpacker. Storyteller. Dreamer. Personal blog of Erika Thea Ajes.
baseurl: "" # the subpath of your site, e.g. /blog/
url: "http://erikathea.github.io" #"http://erikathea.com" # the base hostname & protocol for your site
twitter_username: erikathea
github_username:  erikathea
disqus: codetravel

jekyll-mentions: https://twitter.com

# Build settings
markdown: redcarpet
highlighter: pygments
permalink: pretty

# Set the Sass partials directory, as we're using @imports
sass:
  style: :compressed # You might prefer to minify using :compressed

# Use the following plug-ins
gems:
  - jekyll-sitemap # Create a sitemap using the official Jekyll sitemap gem
  - jekyll-mentions
  - jemoji
  - jekyll-redirect-from
# Exclude these files from your production _site
exclude:
    - Gemfile
    - Gemfile.lock
    - LICENSE
    - README.md

# Front-Matter Defaults
defaults:
  -
    scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
      author: "thea"
